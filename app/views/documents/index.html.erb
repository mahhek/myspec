<% title "Documents" %>

<table>
  <tr>
    <th>Specification Section</th>
    <th>User</th>
    <th>Title</th>
    <th>Note</th>
  </tr>
  <% for document in @documents %>
    <tr>
      <td><%=h document.specification_section_id %></td>
      <td><%=h document.user_id %></td>
      <td><%=h document.title %></td>
      <td><%=h document.note %></td>
      <td><%= link_to "Show", document %></td>
      <td><%= link_to "Edit", edit_document_path(document) %></td>
      <td><%= link_to "Destroy", document, :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
</table>

<p><%= link_to "New Document", new_document_path %></p>
